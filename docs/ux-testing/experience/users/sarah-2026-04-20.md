# Sarah — 2026-04-20

**Flow:** subscription-activation+wallet-top-up
**Score:** UI/UX 2.9/5, Platform 2.0/5

**Key moments:**
- Logged in and landed on raw `/admin/content/calculators` (no wizard auto-redirect) — confusing.
- Wizard appeared only when navigating to `/admin/account`; tile-picker copy was clear and welcoming.
- Stripe Checkout hand-off was fast and branded ("Try Calculators Starter · 14 days free · Then €19.00") — delight.
- After Checkout success, dropped to `/admin/content/account` (raw table) instead of confirmation page — alarming.
- Subscription page shows "Not active" for Calculators despite successful Stripe checkout — webhook pipeline broken; user thinks payment failed.
- AI Assistant low-balance banner + quick-amount dialog worked cleanly (€20/€50/€200).
- Subscription page "Top up" bypassed the quick-amount dialog — inconsistent with banner path.

**Issues hit:**
- Auto-redirect on first login missing (FR1 fail).
- Wizard's PATCH /users/me returned 403 → onboarding state never persists unless admin intervenes.
- Webhook pipeline appears broken — Checkout completes but no local subscription/wallet update.
- Post-Checkout landing URL wrong on both success + cancel.
- User role 403 on `ai_prompts` and `ai_conversations` collections.

**Persona state:** Confused → intrigued by wizard copy → excited at Stripe hand-off → alarmed when subscription page shows "Not active" → would open support ticket or churn.
